// Question No.5 
let famousperson :string ="Shakespeare";
let famousquote:string = '"There is nothing either good or bad, but thinking makes it so"';

// compose message

let message:string = `${famousperson} once said, ${famousquote}`;

console.log(message);
